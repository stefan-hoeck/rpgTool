package efa.rpg.items.dob

//import efa.io.IOCached
//import efa.react.{Source, RTrans, EIn, Out, eTrans}
//import scalaz.effect.IO
//
//object TestEvents {
//  private[this] val ces = IOCached(Source[IO[Unit]])
//
//  def fire: Out[IO[Unit]] = u ⇒ ces.get flatMap (_ fire u)
//
//  def get: IO[Source[IO[Unit]]] = ces.get
//
//  lazy val ein: EIn[IO[Unit]] = eTrans inIO get
//}

// vim: set ts=2 sw=2 et:
